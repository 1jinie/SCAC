# 회의록 - 2026. 08. 18.

# 📅 SCAC 회의록

**Sprint : Week 7**

> RTOS 출입문 연동 · 관리자 장치관리 CRUD 및 운영 상태 관리 · System Log/Memo 개선 · 예약·결제 취소 연동 안정화

## 📅 회의 일시

- 날짜 : 2026.08.18
- 참석자 : 김수영, 장원진, 이지현

## 🚀 프로젝트 진행 현황

- React → Spring → RTOS 장치 Command 처리 흐름 구현
- 비로그인 입실·외출·복귀·퇴실 과정에 Door Open 명령 연동
- RTOS Door Open 후 5초 뒤 자동 Close 처리
- RTOS Health Check Backend 수신 API 구현
- RTOS 기본 Backend Port를 `8888`로 통일
- 관리자 장치 등록·수정·삭제 및 활성/비활성 관리 기능 구현
- 비활성 장치 조회 및 운영 제외 처리 추가
- 관리자 공통 LoadingOverlay 컴포넌트 구현
- 스터디룸 예약 취소 시 연결된 결제도 함께 취소하도록 처리
- 이용권 만료 SMS 반복 실패 시 재시도 제한 기능 추가
- 관리자 Memo 작성자 색상 및 완료 상태 UI 개선
- 주요 관리자 Action의 System Log 기록 범위 확대
- System Log 단일 상세 조회 및 N+1 조회 최적화
- System Log 조회 버튼 및 CSV Download 기능 구현
- Home 진입 시 이전 키오스크 사용자의 상태를 전체 초기화하도록 보완

## 📝 진행 사항

### 👤 이지현

#### Admin Device / CRUD

- 관리자 장치 등록 API 및 Frontend 연결
- 관리자 장치 정보 수정 API 및 Frontend 연결
- 관리자 장치 삭제 API 및 Frontend 연결
- 장치명·장치 유형·설치 위치·IP Address·Serial Number 입력 Form 구현
- Serial Number 중복 검증 추가
- Device Log가 존재하는 장치는 삭제하지 못하도록 처리
- 장치 추가·수정·삭제 처리 후 목록 및 상세 화면 갱신
- 관리자 장치관리 화면 Layout 및 Form UI 개선

#### Device Active / Inactive

- `Device`에 운영 여부를 나타내는 `isActive` 필드 추가
- 신규 장치 생성 시 활성 상태로 처리
- 장치 활성화·비활성화 API 구현
- 기본 장치 목록에서는 활성 장치만 조회하도록 변경
- `includeInactive` 옵션을 통해 비활성 장치 포함 조회 가능하도록 구현
- 관리자 화면에 `비활성 장치 포함` Checkbox 추가
- 비활성 장치를 별도 Style로 표시
- 장치 상세 화면에서 운영 여부 확인 및 활성/비활성 변경 기능 추가
- 비활성 장치에서 전달되는 RTOS Event는 처리하지 않도록 Backend 검증 추가

#### Common UI

- 관리자 공통 `LoadingOverlay` 컴포넌트 구현
- 기존 콘텐츠 위에 Overlay 형태로 Loading UI를 표시하도록 구성
- 로딩 중 Layout 변경을 줄이도록 부모 영역 기준 Loading 처리
- Loading Spinner 및 안내 문구 UI 구현
- `LoadingOverlay` 사용 방법·필수 CSS·Props·예제 주석 추가
- 관리자 장치 목록에 공통 LoadingOverlay 적용

#### Meeting Room / Payment

- 스터디룸 예약과 결제 간 `paymentId` 연결 구조 보완
- 예약 결제 완료 시 해당 예약에 실제 `paymentId` 저장
- 관리자 스터디룸 예약 취소 시 연결된 결제도 함께 취소하도록 `cancelByReservation` 구현
- 예약 취소 시 해당 결제의 결제수단별 취소 Flow 재사용
- 관리자 예약관리 화면의 취소 확인 문구를 예약·결제 동시 취소 기준으로 변경
- 예약 취소 완료 후 예약 목록 재조회
- 기존 `결제 취소 → 예약 취소`에 이어 `예약 취소 → 결제 취소` 방향까지 상태 연동 보완

#### Notification

- 알림 발송 상태에 `RETRY_EXHAUSTED` 추가
- 이용권 만료 SMS 발송 실패 시 최대 재시도 횟수 제한
- 동일 이용권에서 발생한 이전 실패 횟수를 기준으로 Retry Count 관리
- 반복 실패가 최대 횟수에 도달한 경우 더 이상 자동 발송하지 않도록 처리
- `PENDING`, `SUCCESS`, `RETRY_EXHAUSTED` 상태의 기존 알림이 있는 경우 중복 발송 방지
- 현재 이용권의 `startAt` 또는 `createdAt` 이후 알림 이력을 기준으로 중복 및 실패 횟수 판정

#### Store / Refactoring

- `useResetStore()` 반환값을 잘못 사용하던 코드 수정
- `resetAll`, `resetPayData` 구조분해 사용 방식으로 통일
- CancelButton·KioskErrorPage·Home·PaymentResultCard 상태 초기화 호출 정리
- 환경설정 예시 파일명을 `.env.example` 형식으로 정리
- 관리자 좌석 Log Dummy CSV 제거
- Work Log 업데이트

### 👤 김수영

#### SMS / Authentication

- 인증번호 SMS와 이용권 만료 알림의 SOLAPI Client 구조 통합
- SMS Provider 관련 중복 구현 및 설정 정리
- SOLAPI 환경설정을 `solapi.*` 기준으로 일원화
- 인증번호 생성 시 만료된 In-memory 인증정보 제거

#### Admin Memo

- 관리자 ID별 Memo 색상 지정
- Memo 작성자를 색상으로 구분할 수 있도록 UI 개선
- Memo 완료 / 완료 취소 기능 추가
- 완료된 Memo를 흐리게 표시하고 완료 상태 Label 표시
- 기존 삭제 기능은 유지하되 완료 처리 방식 활용 안내 추가
- 검색어 입력 즉시 적용 방식에서 조회 버튼 또는 Enter 입력 시 적용 방식으로 변경

#### System Log

- 관리자 주요 관리 기능에 `@AutoLog` 적용 범위 확대
- 관리자 Memo 등록·수정·삭제 Event 기록
- 장치 상태 변경 Event 기록
- 이용권 생성·수정·판매상태 변경·삭제 Event 기록
- 좌석 상태 변경 및 강제 퇴실 Event 기록
- 결제 취소 및 결제 내역 삭제 Event 기록
- 시스템 로그 최신순 조회 처리
- 로그 검색 조건 입력 후 조회 버튼을 눌러 검색하도록 변경
- 관리자·사용자·시스템 자동 처리의 행위 주체 표시 개선
- System Log 상세 화면의 사용자 전화번호·관리자 Login ID 등 표시 정보 확장
- Excel에서 확인 가능한 CSV 형식의 System Log Download 기능 구현

#### System Log API / Performance

- `GET /api/admin/logs/{logId}` 단일 System Log 상세 조회 API 추가
- Frontend 상세 페이지를 전체 목록 조회 후 검색하는 방식에서 단일 조회 API 방식으로 변경
- `SystemLogRes` 응답 필드 확장
- 사용자 ID와 관리자 ID를 일괄 조회 후 Map으로 변환하도록 구현
- System Log 목록 DTO 변환 시 N+1 조회 방지
- 관리자 Log Detail 데이터 Binding 및 화면 표시 오류 수정

#### Kiosk Session

- Home 화면 진입 시 `resetPayData()` 대신 `resetAll()`을 실행하도록 변경
- 홈 화면에 진입하는 모든 순간 이전 사용자의 로그인 세션 초기화
- 선택 좌석·Check-in·결제·이용권·예약·사용자 정보 초기화
- 공용 키오스크에서 이전 사용자 상태가 남는 문제 방지

#### Admin Authentication

- 관리자 Route에 남아 있던 개발용 임시 인증 주석 제거
- 기존 `AdminPrivateRoute` 적용 구조 정리

### 👤 장원진

#### RTOS / Command

- React → Spring → RTOS Command 처리 구조 구현
- `TaskController`, `TaskCommand`, `TaskStore` 구현
- Command 상태를 `PENDING`, `COMPLETED`, `FAILED`로 관리
- Command 생성 API 구현
- 가장 오래된 대기 Command 조회 API 구현
- Command 상세 조회 API 구현
- RTOS 작업 완료·실패 결과 보고 API 구현
- Command 저장은 In-memory `ConcurrentHashMap` 기반으로 구성

#### Door Control

- Frontend에서 `DOOR_OPEN` Command를 생성하는 `deviceApi` 구현
- 비로그인 좌석 입실 성공 후 Door Open 명령 전송
- 비로그인 외출 후 복귀 시 Door Open 명령 전송
- 비로그인 외출 시 Door Open 명령 전송
- 비로그인 퇴실 시 Door Open 명령 전송
- Door Open Handler 실행 후 5초 동안 Open 상태 유지
- 5초 경과 후 Door Close Handler를 호출하여 자동 Close 처리

#### RTOS Health Check

- RTOS 장치 상태 전송 Payload 보완
- Device ID·Online 상태·Door·Card Reader·Printer 상태 전송
- Backend에 `POST /api/devices/health` Endpoint 추가
- `DeviceHealthRequest` DTO 추가
- RTOS Health Check 요청 수신 및 Logging 처리
- RTOS Command·Device·Fault 관련 Endpoint의 Security 접근 예외 추가

#### RTOS Environment / Receipt

- RTOS 기본 Spring Server Port를 `8080`에서 `8888`로 변경
- Makefile 실행 주소를 Backend Port `8888`로 통일
- RTOS 영수증 정보를 Study Cafe 결제 형태에 맞게 수정
- 영수증에 품목·결제금액·발행일시 표시
- 시간 처리 코드 오류 수정
- Health Check JSON 구조 오류 수정

#### Kiosk UI

- 재입실 완료 Modal에 자동 로그아웃 안내 추가
- 스터디룸 입실 완료 Modal에 자동 로그아웃 안내 추가
- 외출 완료 Modal에 자동 로그아웃 안내 추가
- 퇴실 완료 Modal에 자동 로그아웃 안내 추가

## 💬 주요 논의 및 결정

### 장치 삭제 및 운영 상태 관리

- 관리자 장치 등록·수정·삭제 기능을 구현하는 과정에서 장치 삭제 정책 논의
- 장치를 단순 삭제할 경우 해당 장치와 연결된 Log 데이터 관리에 문제가 발생할 수 있음을 확인
- 실제 운영에서는 장치가 더 이상 사용되지 않더라도 과거 장애 및 운영 기록을 보존할 필요가 있다고 판단
- 장치를 바로 삭제하기보다 운영 여부를 나타내는 상태값을 두는 방향 제안

#### 결정 사항

- `device` 테이블에 `is_active` 컬럼 추가
- 장치 운영 여부를 `true / false`로 관리
- 활성 장치만 기본 관리자 장치 목록에 표시
- 필요 시 비활성 장치까지 함께 조회할 수 있도록 구성
- 운영 종료 장치는 삭제 대신 비활성화를 우선 사용할 수 있도록 기능 제공
- Device Log가 존재하는 장치는 물리 삭제를 제한하여 운영 기록 보존
- 비활성 장치의 RTOS Event는 Backend에서 처리하지 않도록 구성

### 스터디룸 예약 / 결제 취소

- 예약관리에서 스터디룸 예약을 취소할 경우 결제 상태도 함께 정리되어야 함
- 기존의 `결제 취소 → 예약 취소` 처리에 더해 `예약 취소 → 결제 취소` Flow도 연결
- 스터디룸 예약과 결제 상태가 서로 다른 상태로 남지 않도록 양방향 연동 구조 보완

### 키오스크 사용자 상태

- 홈 화면은 다음 사용자가 처음 이용을 시작하는 기준 화면이므로 이전 사용자의 상태가 남지 않도록 전체 초기화가 필요
- Home 진입 시 로그인 정보뿐 아니라 선택 좌석·Check-in·결제·예약·이용권 등 사용자 관련 Store 전체를 초기화하도록 처리

## 🗄️ DB 변경 사항

### `device`

김수영 작업

- `device` 테이블에 `is_active` 컬럼 추가
- Data Type : `TINYINT`
- 장치 운영 여부를 `true / false` 형태로 관리
- Backend `Device.isActive` 필드와 연동
- 관리자 장치 비활성화 기능의 기준 컬럼으로 사용

## 🏢 참여기업 멘토링 피드백 반영

2026.08.14 와이메이커 이창민 이사 멘토링에서 제안된 개선사항을 기능에 반영

- 관리자 장치 CRUD 기능 구현
- 장치 활성/비활성 관리 구조 추가
- 관리자 Memo 작성자별 색상 구분 기능 추가
- Memo 삭제 외 완료 상태 관리 방식 추가
- System Log Event 기록 범위 확대
- System Log 검색을 별도 조회 방식으로 변경
- System Log CSV Download 기능 추가
- RTOS Door Control과 Backend Command 연동 진행

## 👨‍🏫 담당 선생님 피드백

### 2026.08.18 피드백 및 작업 방향

1. 예약 → 결제 → 취소 전체 통합 테스트 진행
2. RTOS 기본 Port `8080`을 Backend Port `8888`과 통일
3. 관리자 예약 API 권한 규칙 및 전체 허용 CORS 설정 정리

## 🧪 QA / 안정화

- RTOS 기본 통신 Port를 `8888`로 통일
- 예약 취소와 결제 취소 연동 구조 보완
- 관리자 장치 CRUD 및 활성/비활성 기능 구현
- Device Log 존재 장치의 삭제 방지
- 비활성 장치 Event 처리 차단
- Home 진입 시 키오스크 사용자 상태 전체 초기화
- System Log 단일 상세 조회 및 N+1 조회 구조 개선
- SMS 반복 실패 시 재시도 횟수 제한
- 관리자 Memo 및 System Log UI/조회 방식 개선

## 📌 Action Items

### 👤 이지현

- 스터디룸 예약 → 결제 → 관리자 예약 취소 전체 통합 테스트
- 예약 취소 후 Payment·Reservation·TicketUsage 상태 확인
- 관리자 장치 등록·수정·삭제 Flow 최종 QA
- 장치 비활성화 및 재활성화 Flow 확인
- 비활성 장치 RTOS Event 차단 확인
- Device Log 존재 장치 삭제 제한 확인
- `RETRY_EXHAUSTED` 알림 상태 및 SMS Retry Flow 테스트
- 관리자 예약 API 권한 규칙 재검토
- 관리자 예약 Endpoint와 CORS 설정 정리

### 👤 김수영

- System Log 단일 조회 및 목록 조회 데이터 검증
- System Log CSV Download 결과 확인
- 관리자 주요 Event Log 기록 확인
- Memo 완료/완료 취소 및 작성자별 색상 UI 확인
- SOLAPI 통합 설정 및 인증번호 발송 Flow 확인
- `device.is_active` DB 컬럼과 Backend Mapping 검증

### 👤 장원진

- React → Spring → RTOS Command 전체 통신 테스트
- 비로그인 입실·외출·복귀·퇴실 Door Open Flow 검증
- Door Open 후 5초 자동 Close 확인
- RTOS Health Check Backend 수신 확인
- Command 완료/실패 보고 Flow 테스트
- RTOS 기본 Port `8888` 적용 상태 확인

### 👥 공통

- 예약 → 결제 → 취소 전체 통합 테스트
- 키오스크 → Backend → RTOS 전체 출입 Flow 테스트
- 관리자 예약 API Security 규칙 정리
- CORS 허용 범위 재검토
- 핵심 기능 최종 시연 시나리오 기준 QA 진행

📝 작성자 : 이지현
