# 회의록 - 2026. 08. 12.

# 📅 SCAC 회의록

**Sprint : Week 6**

> 시간권 외출·복귀 오류 수정 및 키오스크 입·퇴실 안내 UI 개선 · 관리자 권한 오류 보완

## 📅 회의 일시

* 날짜 : 2026.08.12
* 참석자 : 김수영, 장원진


## 🚀 프로젝트 진행 현황

* 시간권 사용자의 외출 후 재입실 불가 오류 수정
* 외출 상태 사용자의 기존 TicketUsage 유지 로직 보완
* 로그인 회원 전용 외출·복귀·퇴실 API 구현
* 회원 퇴실 시 시간권을 다시 `READY` 상태로 전환
* 키오스크 기본 `alert()` 안내를 전용 Modal 방식으로 변경
* 입실·외출·복귀·퇴실 결과 및 오류 안내 UI 개선
* 실제 관리자 Role과 Security 설정 불일치로 발생하던 403 오류 수정
* 프로젝트 막바지 통합 QA 및 오류 수정 진행

## 📝 진행 사항

### 👤 김수영

### 회원 / 인증 / 관리자 권한

#### Admin Authorization

* 관리자 API 요청 시 발생하던 403 권한 오류 원인 확인
* Spring Security의 관리자 Role 검사값 수정
* 기존 `ADMIN` Role 기준 접근 제어를 실제 관리자 데이터에 맞게 변경
* 관리자 전용 `/api/admin/**` 경로 접근 권한을 `SUPER_ADMIN`, `STAFF` 기준으로 수정
* 실제 관리자 Role 데이터와 Backend Security 설정 일치

### 👤 장원진

### 좌석 / 입실·외출·복귀·퇴실

#### Check-in / TicketUsage

* 시간권 사용자가 외출한 후 다시 입실할 수 없던 오류 수정
* 외출 중인 Check-in이 존재하는 경우 새로운 이용권을 찾지 않고 기존 Check-in의 `usageId`를 우선 사용하도록 수정
* 외출 후 복귀 시 기존에 사용하던 시간권을 계속 사용하도록 흐름 보완
* 복수 이용권 우선순위 처리와 외출 복귀 흐름 충돌 방지
* 로그인 회원용 외출 API 추가
* 로그인 회원용 외출 복귀 API 추가
* 로그인 회원용 퇴실 API 추가
* JWT 인증정보에서 현재 사용자를 확인하여 외출·복귀·퇴실 처리
* 회원 퇴실 시 현재 Check-in과 연결된 TicketUsage 조회
* 시간권 퇴실 시 해당 이용권을 `READY` 상태로 변경
* 퇴실 시 좌석 사용자 해제
* 회원 퇴실 SystemLog 기록 처리

#### Frontend / Check-in

* 로그인 회원 외출·복귀·퇴실 API Frontend 연결
* Check-in Store에 회원 전용 외출 처리 추가
* Check-in Store에 회원 전용 복귀 처리 추가
* Check-in Store에 회원 전용 퇴실 처리 추가
* 로그인 후 외출·복귀·퇴실 처리 흐름 수정
* 외출 상태 사용자는 새로운 좌석 선택 없이 바로 복귀하도록 처리

#### Kiosk UI

* 키오스크 안내용 `KioskAlertModal` 공통 컴포넌트 추가
* 브라우저 기본 `alert()` 중심 안내를 키오스크 전용 Modal 방식으로 변경
* 입실 성공·실패 안내 Modal 적용
* 외출 성공·실패 안내 Modal 적용
* 복귀 성공·실패 안내 Modal 적용
* 퇴실 성공·실패 안내 Modal 적용
* 좌석 미선택 및 스터디룸 관련 오류 안내 Modal 적용
* 로그인 사용자 로그아웃 안내 Modal 적용
* 키오스크 화면 크기에 맞춘 Alert Modal 전용 CSS 구현


## 💬 주요 논의 및 결정

* 해당 일자 별도 신규 팀 논의 및 결정 사항 확인되지 않음
* 담당 선생님 피드백을 기준으로 입·퇴실 오류와 관리자 권한 문제를 우선 수정
* 핵심 기능 통합 테스트 과정에서 발견되는 오류를 우선적으로 수정하는 방향 유지

## 🗄️ DB 변경 사항

* 별도 DB 스키마 및 데이터 변경 내역 확인되지 않음
* 기존 관리자 Role 데이터인 `SUPER_ADMIN`, `STAFF`에 맞춰 Backend Security 권한 검사 로직 수정

## 🧪 QA / 안정화

* 시간권 구매 후 외출 → 복귀 과정 오류 수정
* 외출 후 기존 시간권 유지 여부 확인을 위한 Backend 로직 보완
* 로그인 회원 외출·복귀·퇴실 API 분리
* 시간권 퇴실 후 `READY` 상태 복귀 처리 추가
* 키오스크 입실·외출·복귀·퇴실 안내를 전용 Modal UI로 변경
* 실제 관리자 Role과 Backend 권한 설정 불일치로 발생하던 403 오류 수정
* 시간권 퇴실 후 기간권 구매 시 기존 시간권 보유 안내가 잘못 나타나는 문제는 추가 확인 필요
* 관리자 로그인 화면 및 최초 관리자 등록 흐름은 추가 작업 필요

## 👨‍🏫 담당 선생님 피드백

### 키오스크 UI

* 키오스크에서 입실·퇴실 결과를 브라우저 기본 알림으로 표시하지 않고 별도 디자인의 Popup으로 표시
* 입실·외출·복귀·퇴실 성공 및 실패 상태가 UI에서 명확하게 확인되도록 개선

### 이용권 / 입실

* 시간권 구매 후 외출 시 재입실이 불가능한 오류 확인
* 외출 → 복귀 과정이 UI와 Backend에서 명확하게 이어지도록 수정 필요
* 시간권 사용 후 퇴실한 사용자가 기간권을 선택했을 때 기존 사용 가능한 시간권이 있다는 안내가 나타나는 오류 확인
* 이용권 상태와 실제 사용자 흐름에 대한 추가 검증 필요

### 관리자 UI / 인증

* 관리자 로그인 아이디·비밀번호 입력 Form 크기 수정
* 관리자 비밀번호 검증 처리 확인
* 최초 관리자 등록을 위한 화면 필요
* 관리자 API 요청 시 403 오류 원인 확인
* 실제 관리자 Role 데이터에 맞게 Security 권한 검사 수정 필요

## 📌 Action Items

### 👤 김수영

* 변경된 `SUPER_ADMIN`, `STAFF` Role 기준 관리자 API 접근 테스트
* 관리자 Login 이후 주요 관리자 API 403 여부 확인
* 관리자 비밀번호 검증 기능 점검
* 최초 관리자 등록 화면 및 처리 흐름 검토
* 관리자 인증 및 접근 권한 전체 흐름 테스트

### 👤 장원진

* 시간권 구매 → 입실 → 외출 → 복귀 → 퇴실 전체 흐름 테스트
* 외출 상태에서 기존 TicketUsage 유지 여부 검증
* 시간권 퇴실 후 기간권 구매 시 잘못된 기존 이용권 안내 오류 확인
* 복수 이용권 보유 상태에서 외출·복귀 흐름 테스트
* KioskAlertModal 입실·외출·복귀·퇴실 전체 화면 점검
* 비회원 및 로그인 회원 입·퇴실 흐름 비교 테스트

### 👤 이지현

* 해당 일자 결석

### 👥 공통

* 관리자 로그인 및 권한 통합 테스트
* 시간권·기간권 복수 이용권 전환 테스트
* 이용권 만료 알림 통합 테스트
* 인증 → 결제 → 이용권 발급 → 입실 → 외출·복귀 → 퇴실 핵심 사용자 흐름 통합 QA
* Frontend UI 오류 및 Backend 예외 최종 점검

📝 작성자 : 이지현
