# 회의록 - 2026. 07. 23.

# 📅 SCAC 회의록

**Sprint : Week 3**

> 프로젝트 진행률 : **Backend 약 15%**

---

## 📅 회의 일시

- 날짜 : 2026.07.23
- 참석자 : 김수영, 장원진, 이지현

---

## 🚀 프로젝트 진행 현황

- 담당 도메인별 Backend API 구현 진행
- Frontend Dummy Data 제거 및 실제 API 연동 시작
- 공통 API 응답 및 예외 처리 구조 적용
- DB · Entity · Enum 간 데이터 구조 정리
- 핵심 사용자 흐름을 기준으로 기능 연계 개발 시작

---

## 📝 진행 사항

### 👤 이지현 (결제 / 이용권 / 관리자 · 문서 관리)

- Ticket API 구현 및 `scac-front`, `scac-admin` 연동
- Frontend Ticket Dummy Data 제거 및 관련 CSS 수정
- DB 컬럼명에 맞춰 담당 영역 데이터 명칭 수정
- 장치 상태, 로그, 회원 상태 등 Enum 추가
- 전역 `enums` 패키지 구성 및 담당 도메인 Enum 정리
- DB 데이터와 Enum 값 통일
- 공통 `ApiResponse` 및 예외 처리 구조 추가
- 공통 구조 적용에 따른 담당 Service 예외 처리 수정
- Backend README 및 Work Log 업데이트

---

### 👤 김수영 (회원 / 인증 / 권한 · DB 관리)

- User 도메인 API 구현
  - 회원가입 및 비회원 등록
  - 회원 정보 조회
  - 입실 비밀번호 검증 및 변경
- User 요청/응답 DTO 및 Validation 구성
- User Repository 조회 및 검증 로직 구현
- 회원/비회원 비밀번호 암호화 처리
- 사용자 정지 상태 및 패널티 만료 처리 로직 구현
- Auth API 기본 구조 구성
- 관리자 계정 Entity 및 권한 Enum 구성
- DB 구조에 맞춰 User Entity 수정
- DB 샘플 데이터 추가 및 테이블 명칭 `snake_case` 통일
- 비회원 정보 입력 페이지에서 이름 입력 항목 제거

---

### 👤 장원진 (좌석 / 예약 / 입·퇴실 · Git 관리 · 배포 관리)

- 좌석 및 스터디룸 상태값을 DB 기준으로 수정
- Seat 사용자 할당·해제 및 상태 변경 로직 구현
- Seat/Room API에 공통 `ApiResponse` 및 예외 처리 구조 적용
- Ticket_Usage 도메인 구조 구현
  - Entity / DTO / Repository / Service / Controller 구성
- Ticket_Usage 기반 입실 API 구현
- 입실 시 좌석·이용권 검증 및 좌석 상태 변경 로직 구현
- 이용권 유형에 따른 만료 예정 시간 계산 로직 구현
- 입실·외출·복귀·퇴실 상태 변경 로직 구성
- 담당 도메인 비즈니스 예외 처리 추가
- 담당 Enum을 전역 `enums` 패키지로 이동 및 정리
- 입실 화면 뒤로가기 관련 경로 버그 수정
- GitHub 브랜치 병합 및 배포 관리

---

## 💬 논의 사항

- 스터디룸 이용자의 외출 및 퇴실 처리 방식
- Ticket_Usage 상태값 구성
- 공통 API 응답 및 예외 처리 구조
- Backend 기능 구현 순서
- 인증 및 인가 적용 시점

---

## 🎯 결정 사항

- 스터디룸 이용 종료 시 반드시 외출 또는 퇴실 처리를 거치도록 결정
- Ticket_Usage 상태에 `READY` 추가
- Ticket_Usage의 `EXPIRED`와 `EXHAUSTED` 상태를 통합하기로 결정
- API 응답을 `ApiResponse` 기반의 공통 구조로 통일
- 각 도메인에 공통 예외 처리 및 검증 체계를 적용
- 핵심 기능 통합 테스트 이후 인증 및 인가를 적용하기로 결정
- 개별 도메인 단위보다 실제 사용자 흐름을 기준으로 기능을 연계하여 개발하기로 결정

---

## 👨‍🏫 담당 선생님 피드백

### 1. 기능을 세로 기준으로 완성

개별 도메인을 각각 완성하는 방식보다 실제 사용자 흐름을 기준으로 기능을 연결하여 구현할 것.

> **이용권 조회 → 결제 → 사용자 이용권 생성 → 좌석 입실 → 퇴실**

### 2. 공통 API 응답 · 예외 · 검증 체계 구성

- `IllegalArgumentException`과 DTO 직접 반환 방식이 혼재되어 있으므로 구조 통일 필요
- `ApiResponse` 기반의 공통 응답 구조 적용
- 공통 예외 처리 및 검증 체계를 구성한 후 기능 개발 진행

### 3. 인증 및 인가 구현 시점

- 인증 및 인가는 핵심 기능 통합 테스트 이후 적용 가능

---

## 📌 Action Items

### 👤 이지현

- Ticket API 연동 및 기능 검증
- 결제 API 구현
- 결제 → Ticket_Usage 생성 흐름 구현

### 👤 김수영

- User/Auth API 구현 진행
- DB와 Entity 간 구조 정합성 점검
- DB 샘플 데이터 및 구조 관리

### 👤 장원진

- 입실·퇴실 API 구현 진행
- Ticket_Usage와 좌석 이용 흐름 연결
- 좌석 및 스터디룸 상태 관리 기능 구현

### 👥 공통

- `ApiResponse` 및 공통 예외 처리 구조 적용
- 핵심 사용자 흐름 기준 Backend 기능 연계
- 기능 통합 테스트 준비

---

📝 작성자 : 이지현
