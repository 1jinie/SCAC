## 2026.08.03

> 관리자 실제 API 연동 및 인증 → 결제 → 이용권 발급 → 입실 흐름 통합

### 👤 이지현

#### Backend

- 결제 요청 DTO에서 사용자 ID 제거
- JWT 인증 사용자 ID를 기준으로 결제 요청 생성
- 결제 소유자 검증 로직 추가
- 토스 결제 승인 가능 결제수단 검증
- 결제 상태 및 승인 금액 검증
- 토스 승인 결과와 주문 정보 일치 여부 검증
- 토스 결제 승인 후 TicketUsage 자동 발급
- 발급된 TicketUsage를 Payment에 연결
- Mock 카드 승인 후 TicketUsage 자동 발급 및 연결
- 카드 결제와 간편결제 승인 로직 분리
- 결제 취소 사유 앞뒤 공백 제거
- 잘못된 JSON 요청에 대한 `400 Bad Request` 처리
- 잘못된 결제 상태 요청에 대한 `409 Conflict` 처리
- 결제 API에 JWT 인증 정보 적용
- 결제 관련 Security 접근 권한 정리

#### Frontend

- 결제 화면의 사용자 확인 기준을 로그인 인증 여부로 변경
- 오류 화면 홈 이동 시 `replace: true` 적용
- 이용권 목록 로딩 상태 처리
- 이용권 조회 실패 시 다시 시도 및 홈 이동 기능 구현
- 판매 가능한 이용권이 없을 때 빈 화면 안내 추가
- 시간권과 기간권 목록 분류 및 렌더링 보완
- `KioskErrorState` 사용 방법 및 적용 기준 주석 작성
- 오류 화면 Variant 정규화
- 사용하지 않는 개발용 오류 페이지와 CSS 삭제
- 키오스크 오류 화면 크기 및 버튼 스타일 보완

#### QA / Document

- 결제·이용권 및 담당 화면 QA 진행
- 확인된 결제 관련 오류와 예외 처리 보완
- 2026.07.31 Work Log 업데이트
- Spring Boot 버전 업데이트 사유 문서 반영
- 담당 API 명세 점검 및 최신화

### 👤 김수영

#### Backend

- 관리자 계정 생성·목록·상세·수정·삭제 API 구현
- 관리자 로그인 ID 중복 검증
- 관리자 비밀번호 암호화 저장
- 관리자 권한 및 비밀번호 변경 기능 구현
- 관리자 회원 목록 및 조건 검색 기능 구현
- 관리자 회원 상세 조회 구현
- 회원 이용 정지·영구 제재·정지 해제 기능 구현
- 정지 처리 시 정지 종료일 검증
- 기존 UserService를 활용하도록 관리자 회원 Service 구조 정리
- 관리자 좌석 조회·상태 변경·강제 퇴실 API 정리
- 시스템 로그 전체·레벨별 조회 API 구현
- 당일 ERROR·CRITICAL 로그 수 집계 구현
- 당일 누적 결제 매출액 집계 구현
- 회원·좌석·매출·장치·오류 로그를 포함한 대시보드 요약 API 구현
- 관리자 인증 DTO를 request·response 패키지로 정리
- 관리자 인증 API를 제외한 관리자 기능에 권한 설정
- 사용하지 않는 관리자 Service와 임시 파일 제거

#### Frontend

- 관리자 Route에 `AdminPrivateRoute` 적용
- 관리자 로그인·로그아웃 API 응답 처리 보완
- 관리자 대시보드 실제 API 연결
- 관리자 회원관리 정적 데이터 제거
- 회원 제재 Endpoint를 Backend 통합 제재 API에 맞게 수정
- 관리자 API 응답에서 실제 Payload 추출
- 관리자 장치관리 API 경로 수정
- 관리자 예약 API 기본 구조 작성
- 관리자 Frontend와 Backend API 경로 일치 작업

### 👤 장원진

#### Backend

- 로그인 회원용 입실 준비 API 구현
- JWT 인증 정보에서 현재 사용자 ID 조회
- 비로그인·로그인 사용자의 입실 준비 로직 분리
- 공통 사용자·이용권·입실 상태 검증 메서드 구성
- 로그인 회원 ID 조회 API 구현
- Spring Scheduling 활성화
- 1분마다 사용 중·외출 중인 시간권 잔여시간 차감
- 기간권과 READY 상태 이용권을 차감 대상에서 제외
- 잔여시간 소진 시 자동 퇴실 처리
- 자동 퇴실 시 좌석 사용자 해제
- 시간 소진 시 TicketUsage 만료 처리
- 입실·외출·복귀·퇴실 Security 접근 설정 수정

#### Frontend

- 로그인 회원의 입실 준비 API 연결
- 로그인 후 입실 준비를 거쳐 좌석 선택 화면으로 이동
- 로그인 회원 ID를 스터디룸 예약 Store에 저장
- 예약 정보에 사용자 ID를 포함하여 결제 단계로 전달
- 예약 가능 시간 조회 과정의 불필요한 Console 출력 정리

### 🗄️ DB 변경

- 별도 DB 스키마 및 데이터 변경 없음

### 💬 주요 논의 및 결정

- 관리자 로그인은 전화번호가 아닌 `loginId`와 비밀번호를 사용
- 관리자 `loginId`는 영문 소문자로 입력
- DB에 사전 등록된 관리자 계정만 로그인을 허용
- 관리자 Frontend는 Backend 관리자 인증 구조를 기준으로 수정
- Device의 Door 관련 구조는 삭제하지 않고 구현 보류
- 실제 Door 및 장치 연동은 RTOS 연동 단계에서 담당 선생님과 진행
- 관리자 기능은 의존성과 기본 구조를 먼저 생성한 뒤 기능 단위로 구현
- 인증부터 퇴실까지 핵심 사용자 흐름 통합 테스트를 우선 진행

### 👨‍🏫 피드백 반영

- 관리자 기능의 의존성과 기본 코드부터 구성
- 빌드 가능한 상태를 유지하며 기능별 테스트 진행
- 관리자 로그인·로그아웃 임시 로직을 실제 API로 교체
- 관리자 회원관리 정적 데이터를 실제 API로 교체
- 관리자 대시보드 골격을 실제 집계 API로 교체
- 인증 → 결제 → 이용권 발급 → 입실 → 외출·복귀 → 퇴실 통합 테스트
- Backend 빌드 오류와 Frontend 경고 정리
- 결제 오류 우선 수정
- Work Log 및 API 문서 최신화

### 📌 다음 작업

- JWT 기반 결제 전체 흐름 테스트
- 결제 승인 후 TicketUsage 자동 발급 검증
- 결제 소유권 및 결제수단 검증 테스트
- 로그인 회원 입실·외출·복귀·퇴실 통합 테스트
- 시간권 자동 차감 및 자동 퇴실 검증
- 스터디룸 예약 사용자 ID 및 결제 연결 테스트
- 관리자 로그인·로그아웃 실제 API 연결 점검
- 관리자 회원관리·대시보드·계정관리 기능 테스트
- 관리자 권한 및 Private Route 검증
- SystemLog 실제 이벤트 기록 연결
- Backend 전체 빌드 확인
- Frontend 경고 및 Console 출력 정리
- 담당 API 명세 및 Work Log 최신화
- RTOS 장치 연동 규격 확인

---

## 2026.08.04

> 관리자 실제 API 연동 및 자동 퇴실·자동 로그·회원 제재 해제 스케줄러 구현

### 👤 김수영

#### Backend

- 공통 `@AutoLog` Annotation 구현
- AOP 기반 `SystemLogAspect` 구현
- 메서드 실행 결과에 따른 SystemLog 자동 기록
- Security Context에서 사용자 및 관리자 ID 조회
- 요청 정보에서 접속 IP 주소 조회
- 예외 발생 시 ERROR 로그 자동 기록
- 회원 제재 처리 AutoLog 적용
- 관리자 강제 퇴실 AutoLog 적용
- 좌석 상태 수동 변경 AutoLog 적용
- 관리자 계정 생성·수정·삭제 AutoLog 적용
- 제재 기간이 만료된 정지 회원 조회 Query 추가
- 회원 제재 해제 Entity 메서드 구현
- 만료된 회원 제재 자동 해제 Service 구현
- 매시 정각마다 실행되는 제재 해제 스케줄러 구현
- 제재 자동 해제 시 SystemLog 기록
- Spring Scheduling 설정 추가
- 당일 누적 매출액 집계 Repository Query 구현
- PAID 상태의 당일 승인 결제만 합산하도록 개선

#### Frontend

- 관리자 로그인 실제 API 연결
- 관리자 로그인 성공 시 Token 및 사용자 정보 저장
- 관리자 로그인 실패 메시지 처리
- 사용하지 않는 관리자 인증 Store 제거
- 관리자 대시보드 정적 데이터 제거
- 관리자 대시보드 집계 API 연결
- 장치 상태 및 최근 로그 실제 API 연결
- 관리자 회원 제재 API 연결
- 관리자 시스템 로그 정적 데이터 제거
- 시스템 로그 목록 및 상세 API 연결
- 관리자 스터디룸 예약 더미 데이터 제거
- 스터디룸 및 예약 목록 실제 API 연결
- 관리자 예약 취소 API 연결
- 관리자 장치 화면 미정의 변수 오류 수정
- 대시보드 배열 처리 오류 수정

### 👤 장원진

#### Backend

- 시간권 잔여시간 차감 조건 보완
- 시간권 잔여시간 소진 시 자동 퇴실 처리
- 기간권 종료 시각 경과 시 자동 퇴실 처리
- 자동 퇴실 공통 메서드 분리
- 자동 퇴실 시 Check-in 상태 변경
- 자동 퇴실 시 좌석 사용자 해제
- 자동 퇴실 시 TicketUsage 만료 처리
- 이용시간 정보가 없으면 강제 퇴실이 실패하던 오류 수정
- 강제 퇴실 과정의 불필요한 TicketUsage 조회 제거
- 선택 좌석 현재 이용자 조회 API 구현
- 시간권 남은 시간 및 기간권 남은 일수 조회
- 전체 좌석 이용 로그 조회 API 구현
- 특정 좌석 이용 로그 조회 API 구현
- 입실·퇴실·강제 퇴실 SystemLog 기록
- 사용하지 않는 메서드·변수·Import 제거

#### Frontend

- 관리자 좌석 상태 변경 API 연결
- 관리자 강제 퇴실 API 연결
- 선택 좌석 현재 이용자 API 연결
- 좌석관리 임시 사용자 데이터 제거
- 전체 및 선택 좌석 이용 로그 API 연결
- 좌석 로그 응답 필드를 Backend DTO 기준으로 수정
- 사용자 전화번호 형식 변환 적용
- 시간권·기간권별 남은 이용시간 표시
- 분 단위 이용시간을 `n시간 n분` 형식으로 수정

### 🧪 QA

- 참석 팀원을 중심으로 관리자 및 사용자 기능 QA 진행
- 관리자 인증·대시보드·회원관리 기능 점검
- 관리자 좌석 상태 변경 및 강제 퇴실 기능 점검
- 좌석 사용자 정보와 로그 조회 기능 점검
- 관리자 스터디룸 예약 및 취소 기능 점검
- 시간권·기간권 자동 퇴실 기능 점검
- 회원 제재 자동 해제 스케줄러 점검
- SystemLog 자동 기록 및 조회 기능 점검
- 확인된 오류·정적 데이터·미정의 변수 문제 수정
- 세부 테스트 담당자는 확인되지 않아 팀 공통 QA로 기록

### 🗄️ DB 변경

- 별도 DB 스키마 및 데이터 변경 없음

### 💬 주요 논의 및 결정

- 별도 논의 및 결정 사항 없음

### 👨‍🏫 피드백 반영

- 해당 일자 별도 피드백 없음

### 📌 다음 작업

- 관리자 로그인 및 인증 흐름 추가 테스트
- 관리자 대시보드 집계 데이터 검증
- 관리자 회원 제재 및 자동 해제 기능 검증
- AutoLog 사용자·관리자·대상 정보 기록 점검
- 시간권 잔여시간 차감 및 자동 퇴실 테스트
- 기간권 종료 시 자동 퇴실 테스트
- 관리자 강제 퇴실 후 좌석·Check-in 상태 확인
- 관리자 좌석 상태 변경 및 이용 로그 점검
- 관리자 예약 취소 후 화면 상태 갱신 확인
- Backend 빌드 및 Frontend 경고 점검
- 핵심 사용자 흐름 통합 QA
- 프로젝트 사이트 QA 테스트 내역 최신화

---

## 2026.08.05

> 관리자 Figma 시안 제작 및 관리자 도메인·SystemLog·예약관리 구조 안정화

### 👤 이지현

#### Figma / Design

- `scac-admin` JSX·CSS 구조를 기준으로 관리자 페이지 Figma 시안 제작
- Backend 오류로 실제 화면 실행이 어려워 Frontend 소스와 기존 디자인을 기준으로 화면 재구성
- 관리자 데스크톱 공통 레이아웃 제작
- 좌측 사이드바·상단 헤더·페이지 제목·콘텐츠 영역 구성
- 사이드바 전체 메뉴 및 현재 페이지 선택 상태 적용
- 관리자 로그인 기본·입력·오류 상태 화면 제작
- 관리자 메인 대시보드 시안 제작
- 주요 현황 요약 카드·장치 상태·빠른 관리·최근 로그 영역 구성
- 좌석·예약·이용권·회원관리 화면을 위한 공통 프레임 제작
- 관리자 구현 화면의 색상·카드·테두리·배경·간격 규칙 반영

#### Backend / Admin

- 관리자 메모 기능을 Backend `admin` 도메인으로 이동
- 관리자 메모 Controller·DTO·Entity·Repository·Service 패키지 정리
- 관리자 메모 생성 시 JWT 관리자 ID 연결
- 일반 결제 Controller에서 관리자 결제관리 API 분리
- `AdminPaymentController` 생성
- 관리자 결제 목록·상세·취소·삭제 API를 `/api/admin/payments/**`로 통일
- Admin Frontend 결제 API 경로 수정
- Kiosk Frontend에서 관리자 전용 결제관리 API 제거

#### Document / API Specification

- 2026년 8월 Work Log 파일 생성
- 8월 3일·4일 Work Log 작성
- 7월 Work Log 결석자 내역 보완
- 프로젝트 Root README 최신화
- Backend README 최신화
- Kiosk Frontend README 최신화
- Root·Admin·Backend·Kiosk `.env example` 작성
- 환경변수 예시 및 필수 설정값 정리
- 관리자 사이드바에 회원관리 메뉴 추가
- 관리자 회원관리 Route 연결
- 프로젝트 사이트 Payment API 명세 최신화
- 관리자 결제 목록·상세 조회·취소·삭제 API 추가
- 일반 결제와 관리자 결제관리 API 경로 분리 내용 반영

### 👤 김수영

#### Backend

- 사용자 프로필에 실제 활성 이용권명 연결
- `READY`·`USING` 상태의 최근 TicketUsage 조회
- TicketUsage의 `ticketId`로 실제 이용권명 조회
- 관리자 대시보드 당일 매출액 집계 Query 수정
- 관리자 시스템 로그 Controller 통합
- 전체·특정 좌석 로그 조회 Endpoint 통합
- 중복 관리자 로그 Controller 삭제
- 회원 제재 자동 해제 메서드에 `@Transactional` 적용
- SystemLog 저장에 `Propagation.REQUIRES_NEW` 적용
- 비즈니스 로직과 로그 저장 Transaction 분리
- SystemLog 생성 시각에 `@CreationTimestamp` 적용
- 생성 시각 컬럼의 `insertable = false` 제거
- 좌석 로그 사용자 정보 조회 N+1 문제 개선
- 사용자 ID를 수집한 후 `findAllById()`로 일괄 조회
- 좌석 로그 응답 변환 공통 메서드 구성

### 👤 장원진

#### Backend

- 입실 SystemLog가 기록되지 않던 오류 수정
- 퇴실 SystemLog가 기록되지 않던 오류 수정
- `SystemLogService` 의존성 주입 복구
- 관리자용 스터디룸 예약 조회 API 구현
- 관리자 예약관리 응답 DTO 작성
- MeetingRoom·User Join 기반 예약 목록 Query 작성
- 관리자 예약 목록을 생성일 최신순으로 정렬
- 관리자 예약 조회 Service 및 Controller 구현
- 중복 또는 불필요한 AutoLog 적용 제거

#### Frontend

- 관리자 로그 상세 화면의 누락된 `axiosInstance` Import 추가
- 관리자 좌석 상태 변경 처리 구조 정리
- 좌석 상태 변경 및 강제 퇴실 중복 호출 구조 정리
- 관리자 스터디룸 예약관리 화면의 스터디룸 로딩 오류 수정
- 좌석과 스터디룸 데이터를 배치도에 함께 전달
- 관리자 예약 목록 API 연결
- 날짜 및 선택 스터디룸 기준 예약 현황 필터링
- 예약 상태 Label을 Backend Enum 기준으로 수정
- `CONFIRMED`, `CANCELED` 상태 표시 추가
- 사용 중 스터디룸 상태값을 `USR`로 수정
- 사용 중 스터디룸 CSS 미적용 오류 수정
- 좌석관리·예약관리 선택 상태 초기화 처리

#### Git / Stabilization

- GitHub Merge Conflict 해결
- 병합 과정에서 발생한 중복 코드 정리
- 불필요한 Import 및 주석 제거
- Controller·Service 충돌 구조 수정
- Backend 오류 해결을 우선하여 Merge 가능한 상태로 정리

### 🧪 QA / 안정화

- Backend 오류로 인해 별도의 전체 기능 QA는 진행하지 않음
- 오류 발생 부분과 Merge 상태를 중심으로 코드 안정화 진행
- 입실·퇴실 로그 미기록 오류 수정
- 관리자 예약관리 스터디룸 로딩 및 상태 표시 오류 수정
- 정상 빌드 상태 확보 후 전체 통합 QA를 진행하기로 함

### 🗄️ DB 변경

- 별도 DB 스키마 및 데이터 변경 없음
- `payment_table`에 `created_at` 컬럼을 추가하지 않기로 결정
- 당일 매출액은 기존 `paid_at` 컬럼을 기준으로 집계하기로 함

### 💬 주요 논의 및 결정

- 당일 매출액 집계를 위해 신규 `created_at` 컬럼을 추가하지 않기로 함
- 기존 `paid_at` 컬럼과 Entity의 `paidAt` 필드를 활용하기로 결정
- PaymentRepository 당일 매출 Query를 `paidAt` 기준으로 통일하기로 함
- Backend 오류 발생 시 오류를 먼저 수정하고 빌드 확인 후 Merge
- 정상 Merge 이후 다음 기능 작업을 이어가기로 결정

### 👨‍🏫 피드백 반영

- `AdminDashboardController` 참조 및 의존성 주입 오류 수정 필요
- 오류가 발생한 코드를 우선 수정한 뒤 Merge
- Merge 전 Backend 정상 빌드 여부 확인
- 오류가 해결된 최신 코드를 기준으로 후속 작업 진행

### 📌 다음 작업

- `AdminDashboardController` 참조 및 의존성 주입 오류 수정
- PaymentRepository 당일 매출 집계 기준을 `paidAt`으로 수정
- 사용자 프로필 활성 이용권명 조회 테스트
- SystemLog 독립 Transaction 및 생성 시각 저장 확인
- 입실·퇴실 로그 정상 기록 여부 점검
- 관리자 스터디룸 예약 목록 DB 조회 및 필터링 테스트
- 관리자 좌석 상태 변경·강제 퇴실 흐름 점검
- 남은 Git Merge Conflict 및 중복 코드 정리
- 관리자 Figma 세부 화면 및 Prototype 제작
- Payment API 명세 최종 점검
- Backend 정상 빌드 확인 후 전체 통합 QA

---

## 2026.08.06

> 인증·결제·이용권·예약 전체 흐름 보완 및 관리자 인증·자동 스케줄링 안정화

### 👤 이지현

#### Backend

- 로그인 사용자의 결제 상세 조회 API 추가
- JWT 기준 결제 소유자 검증 적용
- 사용자 본인의 결제내역만 조회 가능하도록 처리
- 사용 가능한 좌석 이용권 보유 여부 조회 API 구현
- `READY`·`USING` 상태의 좌석 이용권 존재 여부 조회
- 관리자 장치관리 API를 `AdminDeviceController`로 분리
- 관리자 Device API를 `/api/admin/devices/**`로 정리
- 기존 Device Controller는 RTOS 이벤트 수신 기능 중심으로 정리
- 관리자 이용권 변경 기능을 `AdminTicketController`로 분리
- 관리자 이용권 생성·수정·판매 상태 변경·삭제 API를 `/api/admin/tickets/**`로 정리
- 일반 Ticket Controller를 조회 기능 중심으로 정리
- Security 설정 중복 및 불필요 코드 정리

#### Frontend

- Toss 결제 승인 중복 요청 방지
- Toss 성공·실패 Redirect 처리 분리
- 실패 Redirect에서 불필요한 승인 요청 방지
- `paymentKey`, `orderId`, `amount` 검증
- 결제 승인 오류 메시지 처리 보완
- 기존 이용권 보유 여부 조회 API 연결
- 이용권 목록과 보유 이용권 정보를 함께 조회
- 기존 이용권 보유 시 추가 구매 안내 Modal 구현
- 이용권 미선택 시 안내 Modal 구현
- 조회 실패 시 두 API를 함께 재시도하도록 수정
- 이용권 구매 화면 홈 이동 버튼 추가
- 클릭 UI를 `div`에서 `button`으로 변경
- 공통 오류 Modal 버튼 문구 및 스타일 수정
- 관리자 장치관리 화면 오류 수정
- 관리자 로그인 이동 경로 수정
- 관리자 Sidebar 및 불필요 경로 정리
- 관리자 대시보드 장애·오류 항목 Alert 표시 적용
- Payment Spinner 및 기타 UI 오류 수정

#### Document / Refactoring

- 2026.08.05 Work Log 업데이트
- Frontend Formatter 설정 및 코드 스타일 정리
- 잘못 생성된 경로 및 불필요 파일 제거

### 👤 김수영

#### Frontend / Authentication

- 일반 사용자와 관리자 Refresh API 분리
- 관리자 전용 `/api/admin/auth/refresh` 연결
- 관리자 전용 `/api/admin/auth/logout` 연결
- Axios 401 발생 시 관리자 API 여부에 따라 Refresh Endpoint 분기
- 관리자 Refresh Token 검증 및 재발급 흐름 연결
- 관리자와 일반 사용자 로그아웃 API 분기
- 관리자 로그인 사용자 정보에 실제 Role 및 `isAdmin` 저장
- Access Token·Refresh Token·사용자 정보 LocalStorage 구조 정리
- 관리자 Access Token 만료 여부 확인
- 관리자 Token 만료 시 관리자 전용 Refresh API 호출
- 재발급 성공 시 새 Token 저장
- 재발급 실패 시 인증 정보 초기화
- 관리자 인증 실패 시 로그인 화면 이동
- AdminPrivateRoute 인증 흐름 정리
- 관리자 로그인 Route 경로 불일치 수정
- Router의 중복 `element` 선언 정리
- 중복 회원관리 Route 제거
- 잘못된 관리자 이동 경로 수정
- 인증 관련 이름 및 경로 오류 수정

### 👤 장원진

#### Backend

- 스터디룸 예약 상태 자동 변경 Scheduler 구현
- 1분 간격으로 예약 시작·종료 시간 확인
- `CONFIRMED` 예약 시작 시 `IN_USE`로 변경
- 예약 시작 시 스터디룸 상태를 `USR`로 변경
- 예약 종료 시 `IN_USE`를 `COMPLETED`로 변경
- 다른 이용 중 예약이 없으면 스터디룸 상태를 `AVB`로 변경
- 예약 시작시간·종료시간·상태 조회 Repository 메서드 추가
- 구매일 기준 오래된 사용 가능 이용권 우선 조회
- 사용 중 이용권 만료 후 다음 READY 이용권 자동 활성화
- Check-in 사용 이용권 ID를 다음 이용권으로 변경
- 다음 이용권이 없는 경우 자동 퇴실
- 시간권과 기간권의 시작 처리 로직 분리
- 자동 퇴실 시 좌석 해제 및 SystemLog 기록

#### Frontend

- 관리자 스터디룸 상세 화면에 현재 이용 예약 정보 연결
- 현재 이용자의 전화번호 표시
- 현재 예약 시작·종료시간 표시
- 관리자 예약관리 및 좌석관리의 상태 표기를 `사용중`으로 통일
- 사용자 좌석 화면의 상태 표기 통일

### 🗄️ DB 변경

- 별도 DDL 및 데이터 변경 내역 확인되지 않음
- Entity 변경 사항은 존재하나 실제 DB 스키마 변경 여부는 추가 확인 필요

### 💬 주요 논의 및 결정

- 사용자가 여러 개의 이용권을 구매하는 것을 허용
- 이미 이용권을 보유한 경우 결제 전에 추가 구매 안내 표시
- 사용자가 사용할 이용권을 직접 선택하는 기능은 제공하지 않음
- 이용권 사용 순서는 시스템이 자동 결정

- 시간권 + 시간권
  - 구매일 기준 선입선출 적용

- 시간권 + 기간권
  - 현재 입실 상태이면 현재 사용 중인 시간권 우선
  - 비입실 상태이면 기간권 우선

- 기간권 + 추가 이용권
  - 사용 가능한 기간권 우선 적용

### 👨‍🏫 피드백 반영

- Frontend 중복 `element` 및 Route 정리
- Hook 의존성과 미사용 변수·Import 점검
- 관리자 전용 로그아웃 및 Token Refresh 흐름 보완
- Access Token·Refresh Token 만료 흐름 점검
- 인증 → 결제 → 이용권 발급 → 입실 → 외출·복귀 → 퇴실 전체 통합 테스트 준비

### 📌 다음 작업

- 관리자 인증 만료·재발급·로그아웃 전체 테스트
- 일반 사용자와 관리자 인증 흐름 분리 검증
- Toss 결제 전체 흐름 테스트
- 기존 이용권 보유자의 추가 구매 흐름 테스트
- 시간권 선입선출 및 다음 이용권 자동 전환 검증
- 시간권·기간권 혼합 보유 시 우선순위 구현 확인
- 스터디룸 예약 시작·종료 Scheduler 테스트
- Frontend Warning 및 미사용 코드 정리
- Backend 전체 빌드 확인
- 인증 → 결제 → 이용권 발급 → 입실 → 외출·복귀 → 퇴실 통합 QA

---

## 2026.08.07

> 프로젝트 마무리 단계의 코드 정리 및 기간권 우선 사용·Solapi 이용권 만료 알림 기능 구현

### 👤 이지현

#### Backend

- 실제 구현 범위에 맞추어 결제수단을 `CARD`, `TOSSPAY` 중심으로 정리
- KAKAOPAY·NAVERPAY 관련 Backend 결제 검증 및 취소 로직 제거
- 관리자 결제 취소 정책 정리
- 관리자 결제 검색 로직 보완
- Solapi SDK 의존성 추가
- SMS 발송 Client 구현
- Solapi API Key·Secret·발신번호 환경변수 연결
- `NOTIFICATION_ENABLED`를 통한 문자 발송 활성화 제어
- `NotificationStatus` Enum 구현
  - `PENDING`
  - `SUCCESS`
  - `FAILED`
- `NotificationType` Enum 구현
  - `VERIFICATION`
  - `BARCODE`
  - `EXPIRATION_PREVIEW`
  - `RESERVATION_CONFIRM`
- `NotificationLog` Entity 및 Repository 구현
- 알림 접수·성공·실패 및 외부 메시지 ID 기록
- Solapi 발송 상태 조회 및 알림 상태 동기화 구현
- 시간권 만료 예정 대상 조회 기능 구현
- 기간권 만료 예정 대상 조회 기능 구현
- 이용권 만료 예정 SMS Scheduler 구현
- 동일 이용권의 중복 만료 알림 방지 로직 구현
- 실제 문자 발송 테스트는 미진행 상태

#### Frontend

- 미지원 결제수단 관련 Frontend 상수 및 Label 제거
- 네이버페이 QR 결제 기능 제거
- Kiosk QR 결제 화면 제거
- Mobile Mock 결제 화면 제거
- 관련 Route 및 미사용 Import 제거
- Payment Process의 미사용 결제 분기 제거
- 관리자 결제 검색 로직 보완
- 관리자 Header Route별 제목 정리
- 사용하지 않는 `console.log` 및 Debug 코드 제거
- 관리자·Kiosk의 임시 Mock 데이터 파일 제거
- 사용하지 않는 Admin Check-in Store 제거
- 실제 API 연결 이후 불필요해진 정적 데이터 제거

#### 📄 Document / Presentation

- 프로젝트 Root README 최신화
- 관리자 결제 취소 정책 README 반영
- 2026.08.06 Work Log 작성 및 반영
- 프로젝트 사이트 DB 설계 페이지 최신화
- DB 설계 페이지 테이블명을 영문 소문자로 정리
- 최종 발표용 PPT 초안 작성
- 프로젝트 소개 및 주요 기능을 중심으로 발표 자료 구조 구성

### 👤 장원진

#### Backend

- 이용권 유형·상태별 우선순위 조회 Repository 메서드 추가
- 입실 준비 시 `USING 기간권 → READY 기간권 → READY 시간권` 순서로 조회
- 동일 유형 이용권은 구매일이 오래된 순서로 우선 적용
- 기간권 구매 시 현재 시간권을 READY 상태로 변경하는 로직 추가
- 새 기간권 구매 시 기간권 사용 시작 처리
- 기간권 `validDays` 기준 종료시각 설정
- `TicketUsage.ready()` 구현
- EXPIRED·CANCELED 이용권의 READY 상태 변경 방지
- READY 기간권 입실 시 사용 시작 처리
- 퇴실 시 사용 중 시간권을 READY 상태로 복귀
- 다음 입실에서 남은 시간권을 다시 사용할 수 있도록 처리
- Scheduler의 다음 이용권 활성화 시 시간권·기간권 로직 분리
- 기간권 우선 사용과 Check-in 연동 보완 진행

### 🗄️ DB 변경

- 실제 DB 스키마 및 데이터 변경 없음
- 프로젝트 사이트 DB 설계 페이지의 테이블명 정리
- 한글 설명이 포함된 테이블명에서 설명 문구 제거
- 테이블명을 영문 소문자 기준으로 통일

### 💬 주요 논의 및 결정

- 해당 일자 별도 신규 정책 결정 사항 없음
- 전일 결정한 복수 이용권 사용 우선순위 정책을 기준으로 구현 진행
- 기간권을 시간권보다 우선 적용하는 흐름을 Backend에 반영
- 핵심 기능 구현 완료 단계에 따라 신규 기능보다 코드 안정화와 통합 테스트를 우선

### 👨‍🏫 피드백 반영

- 핵심 기능 대부분 구현 완료에 따라 통합 테스트 단계로 전환
- 미사용 변수·Import·중복 코드·Debug 출력 정리
- 관리자 권한 처리 추가 보완 필요
- 관리자 결제 검색 기능 개선
- 관리자 매출 통계 기능 작업 필요
- 전체 기능의 경고 제거 및 빌드 안정화 진행

### 🧪 QA / 안정화

- Mock 및 정적 데이터 제거 후 실제 API 기반 구조 정리
- 실제 지원 결제수단 기준으로 결제 흐름 정리
- 관리자 결제 검색 오류 수정
- 기간권 우선 사용 및 Check-in 연동 코드 점검
- Solapi 문자 알림 기능은 실제 발송 테스트 미진행
- 관리자 Private Route에 개발 편의를 위한 임시 우회가 남아 있어 최종 인증 검증 필요
- 전체 핵심 사용자 흐름 통합 QA는 추가 진행 필요

### 📌 다음 작업

- Solapi 이용권 만료 알림 실제 발송 테스트
- 알림 발송 상태 동기화 및 중복 방지 검증
- 기간권 구매 시 현재 이용권이 없는 경우 예외 처리 검증
- 기간권 우선 적용과 Check-in 이용권 갱신 흐름 확인
- 시간권·기간권 혼합 보유 시 전체 우선순위 테스트
- 관리자 Private Route 복구 및 권한 검증
- 관리자 결제 검색 기능 최종 점검
- 관리자 매출 통계 기능 보완
- Frontend Warning 및 미사용 코드 제거
- Backend 전체 빌드 확인
- 인증 → 결제 → 이용권 발급 → 입실 → 외출·복귀 → 퇴실 전체 통합 QA
- PPT 발표 자료 세부 내용 및 화면 자료 추가

---

## 2026.08.10

> 이용권 우선 사용 및 전환 로직 안정화 · Solapi 만료 알림 검증 · 관리자 결제 검색 기능 확장

### 👤 이지현

#### Notification / Solapi

- Solapi 환경변수 및 Backend 설정 오류 수정
- Solapi API Key·Secret·발신번호 설정 연결
- 문자 발송 여부를 `NOTIFICATION_ENABLED`로 제어
- 이용권 만료 알림 Scheduler 실행 로그 추가
- 시간권 만료 알림 대상자 및 TicketUsage 정보 로그 추가
- 문자 발송 시작 여부 확인을 위한 로그 추가
- 이용권 만료 예정 문자 실제 발송 테스트 진행
- 테스트 사용자 휴대전화에서 문자 정상 수신 확인
- Solapi 기반 이용권 만료 알림 기본 발송 흐름 검증

#### Admin Payment

- 관리자 결제 내역 검색 기능 확장
- 전화번호 및 결제번호 검색 기능 보완
- 결제 상태 필터 적용
- 결제 수단 필터 추가
  - 카드
  - 토스페이

- 결제 시작일·종료일 기간 필터 추가
- 검색 및 필터 초기화 기능 구현
- 필터 변경 시 Pagination 및 선택 결제 초기화
- 검색·필터 결과를 기준으로 Summary 계산하도록 수정
- 조회 결제 건수·완료 건수·취소 건수 표시
- 필터 결과 중 완료된 결제의 총 결제금액 계산
- 관리자 결제 검색 영역 Layout 및 반응형 CSS 보완

#### Kiosk Payment

- 카드 결제 Mock 처리 방식 변경
- 기존 `Mock 카드 삽입` 버튼 제거
- 카드 결제 화면 진입 후 5초 뒤 Mock 승인 API 자동 호출
- 결제정보가 없는 경우 실패 화면으로 이동
- Mock 승인 성공·실패 결과에 따른 화면 이동 처리
- 실제 카드 단말기 연동 전 시연용 결제 흐름 개선

#### Document / Presentation

- 2026.08.07 Work Log 작성 및 반영
- 프로젝트 결과보고서 제작 진행
- 최종 발표용 PPT 제작 진행
- 프로젝트 구현 기능 및 구조를 기준으로 발표 자료 구성
- 관리자 화면 Figma 시안이 미완성 상태여서 해당 영역을 제외하고 우선 결과보고서 작성

### 👤 김수영

#### User / Authentication

- DB의 `penalty_end_date DATE` 설계에 맞춰 회원 정지 해제 조건 수정
- 정지 종료일 당일부터 회원 상태를 `ACTIVE`로 복구하도록 변경
- 로그인 시 정지 기간이 종료된 회원의 상태를 실시간 정상화
- Refresh Token 재발급 시에도 회원 정지 종료 여부 확인
- 정지 기간이 끝난 회원은 Token 재발급 과정에서도 정상 상태로 복구
- `BANNED` 회원 인증 차단 유지

#### DB

- `system_log` 테이블 Foreign Key 추가
- `meeting_room_reservation` 테이블 Foreign Key 추가

### 👤 장원진

#### TicketUsage / Check-in

- 시간권 → 기간권 자동 전환 시 Check-in의 `usage_id`가 변경되지 않던 오류 수정
- 새 기간권 저장 후 실제 `usageId`를 Check-in에 반영
- `USING`·`AWAY` 상태의 Check-in 이용권 참조 갱신
- 다음 이용권 자동 전환 시 기간권 우선 적용
  - 1순위 : `READY PERIOD_PACK`
  - 2순위 : `READY TIME_PACK`

- 시간권 사용 중 기간권 구매 시 기존 시간권을 `READY` 상태로 변경
- 새 기간권을 우선 사용하도록 처리
- 현재 사용 중인 이용권이 없는 경우 새 기간권을 바로 시작하도록 처리

#### Period Ticket

- 기간권 사용 중 추가 기간권 구매 기능 구현
- 추가 구매한 기간권을 기존 기간권 종료 이후 사용하도록 처리
- 새 기간권의 `startAt`을 기존 기간권의 `endAt`으로 설정
- 새 기간권의 종료시각을 `startAt + validDays` 기준으로 계산
- 기간권 종료시각 계산 기준을 현재시간에서 실제 시작시각 기준으로 수정
- TicketUsage 시작·종료시각 설정 기능 추가
- 여러 기간권을 순차적으로 사용할 수 있도록 이용권 연속 사용 흐름 보완

### 🗄️ DB 변경

- `system_log` 테이블 Foreign Key 추가
- `meeting_room_reservation` 테이블 Foreign Key 추가
- 회원 제재 관련 DB 스키마 변경은 없으며 기존 `penalty_end_date DATE` 구조에 맞춰 Backend 로직 수정

### 💬 주요 논의 및 결정

- 기존 복수 이용권 사용 정책에 따라 기간권을 시간권보다 우선 적용
- 시간권 사용 중 기간권 구매 시 시간권은 `READY` 상태로 보관하고 기간권을 우선 사용
- 기간권 사용 중 추가 기간권 구매 시 기존 기간권 종료 후 이어서 사용
- 이용권 전환 시 Check-in의 `usage_id`도 실제 사용 중인 TicketUsage 기준으로 갱신
- 관리자 결제 검색 기능을 기간·상태·결제수단 필터를 조합할 수 있도록 확장
- 결제 Summary는 전체 결제가 아닌 현재 조회 결과를 기준으로 표시

### 👨‍🏫 피드백 반영

- 로그아웃 API 연결 및 인증 종료 흐름 점검
- 이용권 관련 만료 알림 통합 테스트 진행
- 관리자 결제 내역 검색 기능에 기간 필터 적용
- 관리자 결제 내역 검색 기능에 상태 필터 적용
- 관리자 결제 내역 검색 기능에 결제수단 필터 적용
- 여러 검색 조건을 조합할 수 있도록 관리자 결제관리 기능 확장

### 🧪 QA / 안정화

- Solapi 이용권 만료 예정 문자 실제 발송 테스트
- 테스트 사용자 휴대전화에서 문자 정상 수신 확인
- Solapi 환경설정 및 Scheduler 동작 오류 수정
- 시간권 → 기간권 전환 시 Check-in `usage_id` 미갱신 오류 수정
- 기간권 연속 구매 및 사용기간 계산 로직 보완
- 관리자 결제 검색 및 필터 기능 보완
- 회원 정지 종료일 판정 로직 수정
- 이용권 만료 알림의 시간권·기간권 전체 흐름 및 중복 발송 여부는 추가 통합 테스트 필요

### 📌 다음 작업

- 사용자 및 관리자 로그아웃 API 연결 상태 점검
- 시간권·기간권 만료 알림 전체 통합 테스트
- 동일 이용권 중복 문자 발송 여부 확인
- Solapi 발송 상태 `PENDING → SUCCESS / FAILED` 처리 검증
- 관리자 결제 기간·상태·결제수단 조합 검색 테스트
- 필터 결과와 Summary 집계 결과 일치 여부 확인
- 시간권 → 기간권 전환 시 Check-in `usage_id` 갱신 흐름 테스트
- 기간권 연속 구매 및 다음 기간권 전환 흐름 검증
- 정지 종료일 회원의 로그인 및 Token Refresh 흐름 테스트
- 추가된 Foreign Key 정상 적용 여부 확인
- 인증 → 결제 → 이용권 발급 → 입실 → 외출·복귀 → 퇴실 통합 QA
- 프로젝트 결과보고서 및 발표용 PPT 제작
- 관리자 화면 Figma 제작 후 결과보고서 보완

---

## 2026.08.12

> 시간권 외출·복귀 오류 수정 · 키오스크 입·퇴실 안내 UI 개선 · 관리자 권한 오류 보완

### 👤 김수영

#### Auth / Security

- 관리자 API 요청 시 발생하던 403 권한 오류 수정
- 관리자 Security Role을 실제 DB 데이터에 맞게 변경
- 기존 `ADMIN` 권한 기준을 `SUPER_ADMIN`, `STAFF` 기준으로 수정
- `/api/admin/**` 경로의 관리자 접근 권한 설정 보완

### 👤 장원진

#### Check-in / TicketUsage

- 시간권 사용자의 외출 후 재입실 불가 오류 수정
- 외출 상태의 Check-in이 존재하는 경우 기존 `usageId`의 TicketUsage를 우선 사용하도록 수정
- 외출 복귀 시 기존 사용 이용권을 유지하도록 입실 준비 로직 보완
- 로그인 회원 전용 외출 API 추가
- 로그인 회원 전용 외출 복귀 API 추가
- 로그인 회원 전용 퇴실 API 추가
- JWT 인증정보 기준으로 현재 사용자 외출·복귀·퇴실 처리
- 회원 퇴실 시 현재 Check-in과 연결된 TicketUsage 조회
- 시간권 퇴실 시 이용권을 `READY` 상태로 변경
- 회원 퇴실 시 좌석 사용자 해제 및 SystemLog 기록

#### Frontend

- 회원 전용 외출·복귀·퇴실 API 연결
- Check-in Store에 회원 외출 기능 추가
- Check-in Store에 회원 복귀 기능 추가
- Check-in Store에 회원 퇴실 기능 추가
- 로그인 사용자의 외출·복귀·퇴실 처리 흐름 개선
- 외출 상태 사용자는 별도 좌석 선택 없이 기존 입실 상태로 복귀하도록 처리

#### Kiosk UI

- 공통 `KioskAlertModal` 컴포넌트 구현
- 입실·외출·복귀·퇴실 안내를 브라우저 기본 `alert()`에서 전용 Modal 방식으로 변경
- 입실 성공·실패 안내 Modal 적용
- 외출 성공·실패 안내 Modal 적용
- 복귀 성공·실패 안내 Modal 적용
- 퇴실 성공·실패 안내 Modal 적용
- 좌석 선택 및 스터디룸 오류 안내 Modal 적용
- 로그인 사용자 로그아웃 결과 안내 Modal 적용
- 키오스크 화면에 맞춘 Alert Modal CSS 추가

### 🗄️ DB 변경

- 별도 DB 스키마 및 데이터 변경 없음
- 기존 관리자 Role 데이터(`SUPER_ADMIN`, `STAFF`)에 맞춰 Backend Security 설정 수정

### 💬 주요 논의 및 결정

- 해당 일자 별도 신규 팀 논의 및 결정 사항 없음
- 담당 선생님 피드백을 기준으로 입·퇴실 흐름 오류 및 관리자 접근 권한 문제를 우선 수정
- 통합 테스트 과정에서 발견된 핵심 기능 오류를 우선 보완

### 👨‍🏫 피드백 반영

- 키오스크 입실·퇴실 안내를 별도 디자인 Popup 방식으로 변경
- 시간권 사용자의 외출 후 복귀 불가 오류 수정
- 외출·복귀 과정에서 기존 시간권을 유지하도록 처리
- 관리자 API 403 오류 원인 확인 및 실제 관리자 Role 기준으로 Security 설정 수정
- 관리자 로그인 Form 크기 수정 필요
- 관리자 비밀번호 검증 점검 필요
- 최초 관리자 등록 화면 구현 검토 필요
- 시간권 퇴실 후 기간권 선택 시 기존 시간권 안내 오류 추가 확인 필요

### 🧪 QA / 안정화

- 시간권 외출 → 복귀 흐름 오류 수정
- 로그인 회원 외출·복귀·퇴실 API 연결
- 퇴실 후 시간권 `READY` 상태 복귀 처리
- 키오스크 입·퇴실 안내 Modal UI 적용
- 관리자 Role 불일치로 발생한 403 오류 수정
- 시간권 퇴실 후 기간권 구매 과정의 이용권 안내 오류는 추가 확인 필요
- 관리자 로그인 및 최초 관리자 등록 관련 UI·기능은 추가 작업 필요

### 📌 다음 작업

- 관리자 `SUPER_ADMIN`, `STAFF` 권한 기준 API 접근 테스트
- 관리자 Login 및 주요 관리 기능 403 오류 재확인
- 관리자 로그인 아이디·비밀번호 Form 크기 수정
- 관리자 비밀번호 검증 처리 점검
- 최초 관리자 등록 화면 및 흐름 검토
- 시간권 구매 → 입실 → 외출 → 복귀 → 퇴실 통합 테스트
- 시간권 퇴실 후 기간권 구매 시 잘못된 기존 이용권 안내 오류 수정
- 복수 이용권 자동 전환 통합 테스트
- 이용권 만료 알림 통합 테스트
- 인증 → 결제 → 이용권 발급 → 입실 → 외출·복귀 → 퇴실 전체 통합 QA

---

## 2026.08.13

> 스터디룸 예약·결제 구조 Backend 중심으로 재설계 및 Frontend 연동 · 스터디룸 입실 흐름 개선 · RTOS 장치 연동 준비

### 👤 이지현

#### Meeting Room Reservation / Payment

- 스터디룸 예약 결제 Frontend 연동
- Frontend 중심으로 관리하던 스터디룸 예약 데이터 처리 구조를 Backend 기반으로 변경
- 사용자가 선택한 스터디룸·예약일·시작시간·종료시간을 Backend 예약 API로 전달하도록 수정
- Backend에서 실제 `meeting_room_reservation` 데이터 생성
- 예약 생성 후 반환된 `reservationId`를 Reservation Store에 저장
- `reservationId`를 스터디룸 결제 요청의 기준값으로 사용
- 좌석 이용권 결제와 스터디룸 예약 결제 흐름 분리
- 스터디룸 예약 시간과 시간당 이용금액을 기준으로 최종 결제금액 계산
- 카드 Mock 결제에 스터디룸 예약 결제 연결
- TossPay 결제에 스터디룸 예약 결제 연결
- 결제 승인 후 예약용 TicketUsage 발급 및 예약 확정 흐름 연결

#### Reservation

- 예약 생성 초기 상태를 `PENDING_PAYMENT`로 처리
- `PENDING_PAYMENT` 예약을 중복 예약 검사 대상에 포함
- 결제 진행 중인 예약시간을 예약 가능 시간에서 제외
- 결제 대기 상태에서 30분 이상 경과한 예약을 자동 `CANCELED` 처리하도록 Scheduler 보완
- 결제 완료 시 예약 상태를 `CONFIRMED`로 변경
- 로그인 사용자의 특정 예약 단건 조회 기능 추가

#### Payment Result

- Payment 데이터를 기준으로 좌석 이용권·스터디룸 예약 결제 유형 판별
- 결제 결과 화면의 Store 의존성 축소
- 좌석 이용권 결제 결과 컴포넌트 분리
- 스터디룸 예약 결제 결과 컴포넌트 추가
- 스터디룸 예약 결제 완료 화면에 예약일·이용시간·결제금액 표시
- 결제 완료 후 실제 예약정보를 `reservationId`로 다시 조회하도록 처리

#### Payment Cancel

- 스터디룸 예약 결제 취소 시 연결된 예약도 `CANCELED` 상태로 변경
- 결제 취소와 예약 상태 연동 보완

#### Admin Payment

- 관리자 결제 목록에서 좌석 이용권과 스터디룸 예약 결제 구분
- 스터디룸 예약 결제의 `reservationId` 표시
- 관리자 결제 상세 화면 스터디룸 예약 데이터 호환
- 결제 상품 유형 `MEETING_ROOM` 표시 보완
- PaymentHistoryDTO에 예약·스터디룸 정보 반영
- Payment 조회 Mapper 정리

#### Environment / Refactoring

- Frontend·Admin API 주소 설정을 접속 Host 기준으로 사용할 수 있도록 보완
- 시연 환경을 위한 CORS 설정 보완
- `.env.example` 내용 정리
- 이전 Merge Conflict Marker 제거
- 결제·예약 관련 중간 구현 및 불필요 코드 정리
- Work Log 업데이트

### 👤 장원진

#### Meeting Room Check-in

- 로그인 후 스터디룸 입실 선택 로직 수정
- 현재 로그인 사용자의 이용 가능한 스터디룸 예약 조회 API 구현
- `/api/meeting-rooms/current` Endpoint 추가
- 사용자·현재 날짜·현재 시간·예약 상태를 기준으로 입실 가능한 예약 조회
- Frontend에서 전체 예약 목록을 조회하여 직접 필터링하던 로직 제거
- 현재 예약 판정 로직을 Backend 중심으로 변경
- Reservation Store에 현재 예약 조회 기능 추가
- 스터디룸 입실 시 Backend에서 반환한 현재 예약정보 사용
- 입실 가능한 예약이 없을 경우 오류 Modal 표시
- `ChooseInModal` 버튼 간격 및 Layout CSS 수정
- Inline Style 제거
- 불필요 함수 및 코드 정리

#### RTOS

- Door Open Handler 구현
- Door Close Handler 구현
- `DOOR_OPEN`, `DOOR_CLOSE` Task Handler 등록
- 장치 Health Check Task 구현
- 약 5초마다 Spring Server로 장치 상태를 전달하는 구조 추가
- Device ID·Online 상태·Door 상태·Printer 상태 전송 구조 구성
- FreeRTOS Scheduler에 Health Check Task 등록
- RTOS 측 기능 구현 후 Backend 및 Frontend 연동 작업 필요

### 🗄️ DB 변경

- 별도 DB 스키마 및 데이터 변경 없음
- 기존 스터디룸 예약·Payment·TicketUsage DB 구조를 활용하여 기능 연동

### 💬 주요 논의 및 결정

- 스터디룸 예약 결제를 안정적으로 연결하기 위해 Frontend 중심의 예약 데이터 처리 구조를 Backend 중심으로 재설계
- Frontend는 사용자가 선택한 예약정보를 Backend로 전달하고 실제 예약 데이터 생성은 Backend에서 처리
- Backend에서 반환된 `reservationId`를 결제 및 이후 예약 조회의 기준값으로 사용
- 스터디룸 예약 흐름을 `PENDING_PAYMENT 예약 생성 → 결제 → TicketUsage 발급 → CONFIRMED` 구조로 정리
- 결제 진행 중인 예약시간도 다른 사용자가 선택할 수 없도록 `PENDING_PAYMENT` 상태를 예약 점유 상태에 포함
- 결제 취소 시 연결된 스터디룸 예약도 함께 취소 처리
- RTOS는 Door 제어 및 상태 전송 구조를 먼저 구현하고 Backend·Frontend 연동은 후속 진행

### 👨‍🏫 피드백 반영

- 해당 일자 별도 피드백 없음

### 🧪 QA / 안정화

- 스터디룸 예약·결제 데이터 처리 구조 재정비
- `reservationId` 기반 예약·결제 연결
- 좌석 이용권·스터디룸 예약 결제 분기 로직 보완
- 스터디룸 예약 결제 결과 화면 데이터 호환
- 관리자 결제 화면의 스터디룸 결제 데이터 호환
- 결제 취소 시 예약 상태 연동
- 미결제 예약 자동 만료 처리 추가
- 로그인 사용자 현재 스터디룸 예약 조회 구조 개선
- RTOS 기능은 Backend·Frontend 실제 연동 테스트 필요

### 📌 다음 작업

- 스터디룸 예약 → 결제 → 예약 확정 전체 통합 테스트
- 카드 Mock 스터디룸 예약 결제 검증
- TossPay 스터디룸 예약 결제 검증
- 결제 완료 후 예약용 TicketUsage 발급 확인
- `PENDING_PAYMENT` 30분 만료 처리 테스트
- 동일 시간대 중복 예약 방지 테스트
- 스터디룸 결제 결과 화면 검증
- 관리자 결제 목록·상세의 스터디룸 결제 데이터 확인
- 예약 취소 시 연결 결제 자동 취소 처리 여부 추가 확인
- 스터디룸 입실 전체 흐름 테스트
- RTOS Door Open / Close 처리 테스트
- RTOS Health Check Backend 연동
- 장치관리 Frontend와 RTOS 데이터 연동
- 스터디룸 예약 → 결제 → 입실 → 이용 종료 전체 통합 QA

---

## 2026.08.14

> 시연 환경 안정화 및 주요 기능 QA · 키오스크/관리자 UX 보완 · 참여기업 멘토링 진행

### 👤 이지현

#### Branding / Demo Environment

- Kiosk Browser Title을 `SCAC-KIOSK`로 변경
- Kiosk favicon 및 Manifest Icon을 SCAC Logo로 변경
- Kiosk Manifest의 앱 이름을 SCAC 기준으로 수정
- Admin Browser Title 및 Manifest Branding을 SCAC Admin 기준으로 변경
- Create React App 기본 Meta 및 Template 내용 정리
- 시연 환경에 맞춰 키오스크 화면 Scale을 `0.95`로 조정
- `.env.example` 파일명 및 환경설정 예시 정리

#### Admin / Security

- 관리자 화면에 `AdminPrivateRoute` 재적용
- 인증되지 않은 사용자의 관리자 Layout 접근 보호 구조 복구
- 관리자 예약 조회·취소 관련 Endpoint 권한 설정 보완
- Admin Dashboard SystemLog 조회 Error Logging 보완
- Admin SystemLog Controller Parameter Binding 정리
- Admin Account·Seat·User Controller의 PathVariable Binding 및 코드 정리

#### Meeting Room Reservation

- 스터디룸 예약 가능 시간이 표시되지 않던 오류 수정
- `roomId`, `date`, `reservationId` Request Parameter Binding 명시
- 관리자 예약 화면의 기존 `RESERVED` 상태 기준을 `CONFIRMED` 기준으로 변경
- 관리자 예약 취소 버튼 노출 조건 수정
- 관리자 예약 상태 Badge 및 일별 Schedule 상태 표시 보완
- 스터디룸 예약 결제 취소 시 `/room`, 좌석 이용권 결제 취소 시 `/ticket`으로 이동하도록 Navigation 분기

#### Guest

- 비회원 등록 후 이용권 선택으로 이동하지 않던 오류 수정
- 비회원 계정 생성 후 자동 로그인 처리
- Frontend에서 임의 Check-in 데이터를 생성하던 기존 처리 제거
- 비회원 등록 → 자동 로그인 → 이용권 선택 Flow로 수정
- 전화번호 입력값 정규화 및 Error Handling 보완
- 인증번호 Timer 및 중복 Submit 처리 정리

#### Admin Ticket

- 관리자 이용권 판매상태 변경 API 연결
- 이용권 판매 여부 Toggle 변경 시 Backend 상태 즉시 반영
- 상태 변경 후 이용권 목록 갱신
- `TicketCreateDTO`, `TicketUpdateDTO`, `TicketStatusDTO` Request Binding 구조 보완
- 관리자 이용권 등록·수정·판매상태 변경 오류 수정

#### Payment Result / Session

- 결제 완료 화면 디자인 및 Layout 수정
- 결제 완료 후 `이어서 이용하기` / `로그아웃` 선택 기능 추가
- 10초 Countdown Timer 구현
- Countdown 종료 시 자동 종료 Flow 구성
- 결제 결과 Button 및 자동 로그아웃 안내 UI 추가
- 관리자 로그인 Form Card 및 Input 크기 조정
- `useResetStore` 구조 확장
- 전체 초기화용 `resetAll`과 결제 관련 초기화용 `resetPayData` 분리
- `resetAll`에 Payment·Ticket·Reservation·Check-in·Seat 선택·User 정보 초기화 및 Logout 처리 추가
- `resetPayData`는 Payment·Ticket·Reservation 데이터만 초기화하도록 구성
- `이어서 이용하기` 선택 시 결제 관련 데이터만 초기화하고 LoginHome으로 이동
- 키오스크 사용 종료 시 이전 사용자 Store 및 인증정보가 남지 않도록 초기화 범위 보완

#### Documentation

- Root README 최신화
- Backend README v3.0 작성
- Admin README 현행화
- 최신 인증·결제·예약·TicketUsage 구조 반영
- 최신 API Endpoint 및 Security 정책 정리
- Toss Payments·SOLAPI 연동 내용 정리
- Scheduler 및 환경변수 구성 정리
- JPA / MyBatis 역할 및 Backend 구조 문서화
- RTOS 연동 현황 및 미구현 Endpoint 정리
- Known Issues 및 Pending Task 정리
- `docs/worklog/2026-08.md` 업데이트

#### Presentation / Mentoring

- 참여기업 와이메이커 이창민 이사 대상 SCAC Kiosk 및 Admin 주요 기능 시연
- 키오스크 예약·결제·입실 흐름과 관리자 기능 중심으로 프로젝트 구현 현황 설명
- 시연 과정에서 발견된 기능 및 UI 오류 항목 확인
- 관리자 장치·결제·Memo·System Log 기능에 대한 실무 개선 의견 수렴

### 👤 장원진

#### RTOS / Device

- RTOS 명령 연동을 위한 Backend 기본 구조 생성
- `DeviceCommand` Entity 파일 추가
- `DeviceCommandRepository` 추가
- `DeviceCommandResponse` DTO 추가
- `DeviceCommandFinishReq` DTO 추가
- RTOS Command Backend 구현을 위한 골격 구성
- 실제 Command API 및 Frontend 연동은 후속 작업으로 진행

#### Meeting Room Reservation

- 사용 중인 스터디룸도 다른 시간대 예약을 위해 선택할 수 있도록 Room 선택 조건 수정
- 현재 Room 상태가 `available`이 아닌 경우 선택을 막던 조건 제거
- 실제 예약 가능 여부를 날짜·시간 예약 검사에서 판단하도록 UI 흐름 보완

#### Kiosk UI

- 로그인 성공 안내를 `alert()`에서 `KioskAlertModal`로 변경
- 로그아웃 성공 안내를 `KioskAlertModal`로 변경
- Modal 확인 후 홈으로 이동하도록 Navigation 보완

#### TicketUsage

- 시간권 보유 상태에서 기간권 구매 시 발생하던 TicketUsage 연결 오류 수정
- 새 기간권 TicketUsage 저장 전 Check-in의 `usageId`를 변경하던 처리 제거
- 저장된 TicketUsage 기준으로 이용권 연결이 처리되도록 흐름 보완

#### Check-in / Authentication

- 좌석 입실 완료 후 Logout 처리
- 스터디룸 입실 완료 후 Logout 처리
- 외출 완료 후 Logout 처리
- 외출 후 복귀 완료 시 Logout 처리
- 퇴실 완료 후 Logout 처리
- 키오스크 사용 처리 이후 이전 사용자의 인증정보가 남지 않도록 보완

#### Navigation

- 이용권 구매 화면의 `홈으로 돌아가기` 버튼을 `뒤로가기`로 변경
- 이용권 구매 Flow에서 직전 화면으로 돌아가도록 Navigation 수정

#### Presentation / Mentoring

- 참여기업 대상 프로젝트 시연 지원
- 일부 키오스크 기능 설명 및 시연 보조
- 와이메이커 이창민 이사 멘토링 주요 조언 및 개선사항 정리

### 🗄️ DB 변경

- 별도 DB 스키마 및 데이터 변경 없음
- 기존 DB 구조를 기준으로 기능 오류 및 연동 로직 수정

### 💬 주요 논의 및 결정

- 키오스크 사용 완료 후 이전 사용자의 인증·선택·결제 상태가 남지 않도록 Store 초기화 범위 확대
- 결제 후 계속 이용하는 경우와 완전히 종료하는 경우를 구분하기 위해 `resetPayData`와 `resetAll`을 분리
- 키오스크 사용자 안내를 Browser 기본 Alert보다 공통 Modal 방식으로 통일
- 현재 사용 중인 스터디룸도 다른 시간대 예약이 가능하므로 Room의 현재 상태만으로 선택을 제한하지 않도록 변경
- RTOS 연동을 위해 Backend DeviceCommand 구조를 단계적으로 추가
- 참여기업 시연 과정에서 확인된 오류 및 실무 개선 의견을 이후 QA 항목으로 관리

### 🏢 참여기업 특강 및 멘토링

- 와이메이커 이창민 이사의 솔루션 비교분석 특강 및 프로젝트 멘토링 진행
- SCAC Kiosk 및 Admin 구현 기능 시연
- 자동 로그아웃 Modal 및 결제 완료 화면 Layout 개선 의견 수렴
- 관리자 이용권·결제 취소 오류 점검 필요사항 확인
- 장치 장애 실시간 Event 및 장치 CRUD 기능 확장 의견 수렴
- 회원 상태 변경 시 문자 알림 기능 검토 의견 수렴
- Memo 작성자 식별 및 완료 상태 UI 개선 의견 수렴
- System Log Event 확대 및 대량 조회 방식 개선 의견 수렴
- Log Excel Download 기능 추가 의견 수렴

### 👨‍🏫 피드백 반영

- 해당 일자 담당 선생님 별도 피드백 기록 없음
- 참여기업 멘토링 피드백을 기준으로 시연 기능의 추가 개선사항 정리

### 🧪 QA / 안정화

- AdminPrivateRoute 재적용
- 관리자 SystemLog 오류 수정
- 스터디룸 예약 가능 시간 조회 오류 수정
- 관리자 예약 상태 기준 수정
- 비회원 등록 → 자동 로그인 → 이용권 선택 Flow 수정
- 관리자 이용권 등록·수정·판매상태 변경 오류 보완
- 시간권 보유 상태에서 기간권 구매 시 TicketUsage 연결 오류 수정
- 결제 완료 화면 및 자동 종료 UX 개선
- 키오스크 상태 초기화 범위 보완
- 입실·외출·복귀·퇴실 완료 후 인증정보 정리
- 참여기업 시연을 통해 추가 기능 오류 및 개선사항 확인

### 📌 다음 작업

- 키오스크 전체 기능 Flow 통합 테스트
- 관리자 이용권 등록·수정·삭제 재검증
- 관리자 결제 취소 오류 재검증
- 취소 결제 내역 UI 구분 방식 검토
- 결제 완료 화면 Layout 및 자동 로그아웃 Flow 검증
- 비회원 등록 → 이용권 구매 전체 Flow 테스트
- `resetAll`, `resetPayData` 초기화 범위 검증
- RTOS DeviceCommand Backend 실제 구현
- RTOS 장치 장애 실시간 Event 시나리오 검토
- 회원 상태 변경 문자 알림 기능 검토
- 관리자 장치 CRUD 기능 검토
- Memo 작성자 식별 및 완료 상태 UI 검토
- System Log Event 기록 범위 및 대량 조회 방식 검토
- Log Excel Download 기능 검토
- 최신 README·API·실제 코드 정합성 최종 확인

---

## 2026.08.18

> RTOS 출입문 연동 · 관리자 장치관리 CRUD 및 운영 상태 관리 · System Log/Memo 개선 · 예약·결제 취소 연동 안정화

### 👤 이지현

#### Admin Device

- 관리자 장치 등록·수정·삭제 API 및 Frontend 연동
- 장치명·유형·설치 위치·IP Address·Serial Number 입력 Form 구현
- Serial Number 중복 검증 추가
- Device Log가 존재하는 장치는 삭제하지 못하도록 처리
- 장치 추가·수정·삭제 후 관리자 장치 목록 및 상세정보 갱신
- 장치관리 화면 Form 및 Button UI 보완

#### Device Active / Inactive

- 장치 운영 여부 관리를 위한 `isActive` 필드 추가
- 장치 활성화·비활성화 API 구현
- 기본 장치 목록에서는 활성 장치만 조회하도록 변경
- `includeInactive` 옵션으로 비활성 장치 포함 조회 지원
- 관리자 화면에 `비활성 장치 포함` Filter 추가
- 비활성 장치를 별도 Style로 표시
- 장치 상세 화면에 운영 여부 표시 및 활성/비활성 Button 추가
- 비활성 장치의 RTOS Event 처리 차단

#### Common UI

- 관리자 공통 `LoadingOverlay` 컴포넌트 구현
- 장치 목록 조회에 Overlay Loading UI 적용
- Loading Spinner 및 안내 문구 Style 추가
- `LoadingOverlay` 사용 방법·필수 CSS·Props·사용 예제 주석 추가

#### Meeting Room / Payment

- 스터디룸 결제 완료 시 예약 데이터에 `paymentId` 연결
- 관리자 스터디룸 예약 취소 시 연결된 Payment도 함께 취소하도록 처리
- `PaymentService.cancelByReservation()` 구현
- 관리자 예약 취소 UI를 예약·결제 동시 취소 Flow에 맞게 수정
- 기존 결제 취소 → 예약 취소에 이어 예약 취소 → 결제 취소 방향까지 상태 연동 보완

#### Notification

- `NotificationStatus.RETRY_EXHAUSTED` 추가
- 이용권 만료 SMS 실패 시 최대 재시도 횟수 제한
- 동일 이용권 기준으로 이전 발송 실패 횟수 확인
- 최대 실패 횟수 도달 시 추가 SMS 발송 중단
- `PENDING`, `SUCCESS`, `RETRY_EXHAUSTED` 상태의 중복 알림 발송 방지
- 이용권 `startAt` 또는 `createdAt` 이후 Log를 기준으로 알림 이력 판정

#### Refactoring / Cleanup

- `useResetStore` 반환값 사용 오류 수정
- `resetAll`, `resetPayData` 구조분해 사용 방식으로 통일
- CancelButton·KioskErrorPage·Home·PaymentResultCard 상태 초기화 코드 정리
- `.env.example` 파일명 정리
- 관리자 Seat Log Dummy CSV 제거
- Work Log 업데이트

### 👤 김수영

#### SMS / Authentication

- 본인인증 SMS와 이용권 만료 알림의 SOLAPI Client 구조 통합
- 중복 SMS Provider 설정 제거
- SOLAPI 환경설정을 `solapi.*` 기준으로 일원화
- 인증번호 생성 시 만료된 In-memory 인증정보 제거

#### Admin Memo

- 관리자 ID별 Memo 색상 지정
- Memo 작성자를 색상으로 구분하도록 UI 개선
- Memo 완료 / 완료 취소 기능 추가
- 완료된 Memo를 흐리게 표시하고 완료 상태 Label 추가
- 기존 삭제 기능 유지 및 완료 처리 활용 안내 추가
- Memo 검색을 조회 버튼 또는 Enter 입력 시 적용하도록 변경

#### System Log

- 관리자 주요 관리 Action에 `@AutoLog` 적용 범위 확대
- Memo·Device·Ticket·Seat·Payment 관련 주요 관리 Event Logging 추가
- System Log 최신순 조회
- 검색 조건 입력 후 조회 버튼으로 검색하도록 변경
- 관리자·사용자·시스템 행위 주체 표시 개선
- System Log CSV Download 기능 추가
- 단일 System Log 상세 조회 API 추가
- Admin Log Detail을 단일 조회 API 기반으로 변경
- SystemLog 응답에 사용자 전화번호·관리자 Login ID 등 필드 추가
- 사용자·관리자 정보를 일괄 조회하도록 변경하여 N+1 조회 방지

#### Kiosk Session

- Home 진입 시 `resetPayData` 대신 `resetAll` 실행
- 로그인 세션·좌석·Check-in·결제·예약·이용권·사용자 정보 전체 초기화
- 이전 키오스크 사용자의 상태가 다음 사용자에게 남지 않도록 보완

#### Admin Authentication

- 관리자 Route에 남아 있던 개발용 임시 인증 주석 제거

### 👤 장원진

#### RTOS / Command

- React → Spring → RTOS Command 처리 구조 구현
- `TaskController`, `TaskCommand`, `TaskStore` 추가
- Command 상태 `PENDING`, `COMPLETED`, `FAILED` 관리
- Command 생성·대기 조회·상세 조회·처리 결과 보고 API 구현
- Command 정보를 In-memory `ConcurrentHashMap`으로 관리

#### Door Control

- Frontend `openDoor()` API 구현
- 비로그인 좌석 입실 완료 후 Door Open Command 전송
- 비로그인 외출·복귀·퇴실 완료 후 Door Open Command 전송
- Door Open 후 5초 유지
- 5초 경과 후 Door Close Handler 자동 실행

#### RTOS Health Check

- Backend `/api/devices/health` Endpoint 구현
- `DeviceHealthRequest` DTO 추가
- Device ID·Online 상태·Door·Card Reader·Printer 상태 수신
- RTOS Command·Device·Fault Endpoint Security 접근 예외 추가

#### RTOS Environment / Receipt

- RTOS 기본 Backend Port를 `8080`에서 `8888`로 변경
- Makefile 실행 Port를 `8888`로 통일
- RTOS 영수증 출력정보를 스터디카페 결제 형태로 수정
- 품목·결제금액·발행일시 표시
- 시간 처리 및 Health Check JSON 오류 수정

#### Kiosk UI

- 재입실·스터디룸 입실·외출·퇴실 완료 Modal에 자동 로그아웃 안내 문구 추가

### 🗄️ DB 변경

- `device` 테이블에 `is_active` 컬럼 추가
- Data Type : `TINYINT`
- 장치 운영 여부를 `true / false` 값으로 관리
- 관리자 장치 활성/비활성 기능과 Backend `isActive` 필드 연동
- DB 수정 : 김수영

### 💬 주요 논의 및 결정

- 장치 삭제 시 과거 Device Log 보존 문제를 고려하여 물리 삭제보다 비활성화 방식 활용 논의
- `device` 테이블에 `is_active` 컬럼을 추가하기로 결정
- 활성 장치만 기본 목록에 표시하고 필요 시 비활성 장치를 함께 조회하도록 구성
- Device Log가 존재하는 장치는 삭제를 제한하고 비활성화를 이용하도록 처리
- 비활성 장치에서는 RTOS Event를 처리하지 않도록 구성
- 스터디룸 예약과 결제가 서로 다른 상태로 남지 않도록 예약 취소 → 결제 취소 방향까지 연결
- Home 진입 시 이전 사용자의 로그인·좌석·결제·예약 등의 상태를 전체 초기화하도록 처리

### 🏢 참여기업 멘토링 피드백 반영

- 관리자 장치 CRUD 기능 구현
- 장치 운영 상태 관리 기능 추가
- 관리자 Memo 작성자별 색상 구분
- Memo 완료 상태 관리 기능 추가
- 관리자 주요 Action System Log Event 확대
- Log 조회 버튼 방식 적용
- System Log CSV Download 기능 추가
- RTOS Door Control 연동 진행

### 👨‍🏫 피드백 반영

- 예약 → 결제 → 취소 전체 통합 테스트 필요사항 확인
- RTOS 기본 Port를 Backend Port `8888`로 통일
- 관리자 예약 API 권한 규칙 및 CORS 설정 정리 필요사항 확인

### 🧪 QA / 안정화

- 예약 취소와 연결 Payment 자동 취소 처리 추가
- RTOS 통신 Port `8888` 통일
- 관리자 장치 CRUD 기능 구현
- 장치 활성/비활성 운영 상태 관리 추가
- Device Log 존재 장치 삭제 제한
- 비활성 장치 RTOS Event 차단
- Home 진입 시 사용자 상태 전체 초기화
- SMS 반복 실패 재시도 제한
- System Log 조회 구조 및 N+1 조회 개선

### 📌 다음 작업

- 예약 → 결제 → 취소 전체 통합 테스트
- 관리자 예약 취소 후 Payment·Reservation·TicketUsage 상태 검증
- 관리자 예약 API Security 권한 규칙 정리
- CORS 전체 허용 범위 점검 및 정리
- RTOS React → Spring → Device Command 전체 통신 테스트
- Door Open 후 5초 자동 Close Flow 검증
- RTOS Health Check 수신 확인
- 관리자 장치 CRUD 전체 Flow 테스트
- 장치 비활성화 및 재활성화 테스트
- Device Log 존재 장치 삭제 차단 검증
- `RETRY_EXHAUSTED` SMS Retry Flow 검증
- System Log 및 Memo 개선 기능 QA
- 최종 시연 시나리오 기준 핵심 기능 통합 테스트

---
